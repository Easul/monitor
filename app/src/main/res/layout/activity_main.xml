<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_warm_gradient"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="若轻看护"
            android:textColor="@color/warm_text"
            android:textSize="34sp"
            android:textStyle="bold" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:text="通过 EasyTier 连接家里的设备，打开摄像头与语音陪伴。"
            android:textColor="@color/warm_text_secondary"
            android:textSize="15sp" />

        <TextView
            android:id="@+id/statusText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="18dp"
            android:background="@drawable/bg_status_pill"
            android:text="请先配置并启动 EasyTier VPN，然后选择控制端或被控端模式。"
            android:textColor="@color/warm_text_secondary"
            android:textSize="14sp" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="22dp"
            android:background="@drawable/bg_card"
            android:orientation="vertical">

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="网络"
                android:textColor="@color/warm_text"
                android:textSize="20sp"
                android:textStyle="bold" />

            <Button
                android:id="@+id/settingsButton"
                android:layout_width="match_parent"
                android:layout_height="52dp"
                android:layout_marginTop="14dp"
                android:background="@drawable/bg_secondary_button"
                android:text="EasyTier 设置"
                android:textColor="@color/warm_text" 
                android:elevation="0dp"
                android:stateListAnimator="@null"
                android:translationZ="0dp" />

            <Button
                android:id="@+id/startVpnButton"
                android:layout_width="match_parent"
                android:layout_height="52dp"
                android:layout_marginTop="10dp"
                android:background="@drawable/bg_primary_button"
                android:text="启动 EasyTier VPN"
                android:textColor="#FFFFFF" 
                android:elevation="0dp"
                android:stateListAnimator="@null"
                android:translationZ="0dp" />

            <Button
                android:id="@+id/stopVpnButton"
                android:layout_width="match_parent"
                android:layout_height="52dp"
                android:layout_marginTop="10dp"
                android:background="@drawable/bg_secondary_button"
                android:text="停止 EasyTier VPN"
                android:textColor="@color/warm_text" 
                android:elevation="0dp"
                android:stateListAnimator="@null"
                android:translationZ="0dp" />

            <Button
                android:id="@+id/exitAllButton"
                android:layout_width="match_parent"
                android:layout_height="52dp"
                android:layout_marginTop="10dp"
                android:background="@drawable/bg_secondary_button"
                android:elevation="0dp"
                android:stateListAnimator="@null"
                android:text="退出全部连接"
                android:textColor="@color/warm_text"
                android:translationZ="0dp" />
        </LinearLayout>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="18dp"
            android:background="@drawable/bg_card"
            android:orientation="vertical">

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="通话"
                android:textColor="@color/warm_text"
                android:textSize="20sp"
                android:textStyle="bold" />

            <Button
                android:id="@+id/controlledListenButton"
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:layout_marginTop="14dp"
                android:background="@drawable/bg_primary_button"
                android:text="被控端：等待连接并发送摄像头"
                android:textColor="#FFFFFF" 
                android:elevation="0dp"
                android:stateListAnimator="@null"
                android:translationZ="0dp" />

            <Button
                android:id="@+id/devicesButton"
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:layout_marginTop="10dp"
                android:background="@drawable/bg_secondary_button"
                android:text="控制端：连接被控设备"
                android:textColor="@color/warm_text" 
                android:elevation="0dp"
                android:stateListAnimator="@null"
                android:translationZ="0dp" />
        </LinearLayout>
    </LinearLayout>
</ScrollView>
